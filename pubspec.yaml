name: youtube_video_info
description: This is a lightweight package to fetch video info for YouTube from the url
version: 1.0.2
homepage: https://github.com/kaex/youtube_video_info

environment:
  sdk: ">=2.12.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  html: ^0.15.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: